# LAYER_CONF_VERSION is increased each time build/conf/bblayers.conf
# changes incompatibly
LCONF_VERSION = "6"

TOPDIR := "${@os.path.dirname(os.path.dirname(FILE))}"
BBPATH = '${TOPDIR}:${HOME}/.oe'
BBFILES = ''

BBLAYERS ?= "\
    ${OEDIR}/meta-sourcery \
    ${OEDIR}/meta-mentor \
    ${OEDIR}/meta-oe/meta-oe \
    ${OEDIR}/oe-core/meta \
    ${OEDIR}/meta-yocto \
    ${OEDIR}/meta-intel \
"
